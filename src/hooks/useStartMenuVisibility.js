import { useEffect, useState } from 'react';

function useStartMenuVisibility() {
  return useState(false);
  // const [isOpen, setIsOpenState] = useState(false);

  // return [isOpen, setIsOpenState];
}

export default useStartMenuVisibility;
